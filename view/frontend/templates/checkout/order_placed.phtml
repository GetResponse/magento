<?php

declare(strict_types=1);

use GetResponse\GetResponseIntegration\Block\Checkout\OrderPlaced;

/** @var OrderPlaced $block */
$order = $block->getBufferedOrder()['order'];

if (!empty($order)) {
    $orderJson = json_encode($order, JSON_HEX_TAG | JSON_HEX_APOS | JSON_HEX_QUOT | JSON_HEX_AMP);
    echo '<script type="text/javascript">const GrOrderPlaced = ' . $orderJson . ';</script>';
}
