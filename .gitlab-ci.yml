
stages:
  - install_dependencies
  - test

.exclude-branch-develop:
  rules:
    - if: $CI_COMMIT_BRANCH == "develop"
      when: never
    - when: on_success

composer_install:php72:
  image: magento/magento-cloud-docker-php:7.2-cli-1.3.5
  stage: install_dependencies
  tags:
    - hq1-small-amd64
  before_script:
    - composer config platform.php 7.2.34
  script:
    - composer self-update --2
    - composer config -a -g http-basic.repo.magento.com $COMPOSER_REPO_MAGENTO_USERNAME $COMPOSER_REPO_MAGENTO_PASSWORD
    - composer config --no-interaction audit.block-insecure false
    - composer install --prefer-dist --verbose
  artifacts:
    paths:
      - vendor/
    expire_in: 2 hrs
  rules:
    - !reference [.exclude-branch-develop, rules]

test:phpunit:php72:
  needs: ['composer_install:php72']
  image: magento/magento-cloud-docker-php:7.2-cli-1.3.5
  stage: test
  tags:
    - hq1-small-amd64
  script:
    - ./vendor/bin/phpunit --testsuite unit --configuration ./Test/phpunit8.xml --colors=never
  rules:
    - !reference [.exclude-branch-develop, rules]
  dependencies:
    - composer_install:php72

composer_install:php81:
  image: magento/magento-cloud-docker-php:8.1-cli-1.4.6
  stage: install_dependencies
  tags:
    - hq1-small-amd64
  before_script:
    - docker-php-ext-enable sodium
  script:
    - composer config -a -g http-basic.repo.magento.com $COMPOSER_REPO_MAGENTO_USERNAME $COMPOSER_REPO_MAGENTO_PASSWORD
    - composer install --prefer-dist --verbose
  artifacts:
    paths:
      - vendor/
  rules:
    - !reference [ .exclude-branch-develop, rules ]

test:phpunit:php81:
  needs: [ 'composer_install:php81' ]
  image: magento/magento-cloud-docker-php:8.1-cli-1.4.6
  stage: test
  tags:
    - hq1-small-amd64
  before_script:
    - docker-php-ext-enable sodium
  script:
    - ./vendor/bin/phpunit --testsuite unit --configuration ./Test/phpunit9.xml --colors=never
  rules:
    - !reference [ .exclude-branch-develop, rules ]
  dependencies:
    - composer_install:php81

composer_install:php83:
  image: magento/magento-cloud-docker-php:8.3-cli-1.3.7
  stage: install_dependencies
  tags:
    - hq1-small-amd64
  before_script:
    - docker-php-ext-enable sodium
  script:
    - composer config -a -g http-basic.repo.magento.com $COMPOSER_REPO_MAGENTO_USERNAME $COMPOSER_REPO_MAGENTO_PASSWORD
    - composer install --prefer-dist --verbose
  artifacts:
    paths:
      - vendor/
  rules:
    - !reference [ .exclude-branch-develop, rules ]

test:phpunit:php83:
  needs: [ 'composer_install:php83' ]
  image: magento/magento-cloud-docker-php:8.3-cli-1.3.7
  stage: test
  tags:
    - hq1-small-amd64
  before_script:
    - docker-php-ext-enable sodium
  script:
    - ./vendor/bin/phpunit --testsuite unit --configuration ./Test/phpunit9.xml --colors=never
  rules:
    - !reference [ .exclude-branch-develop, rules ]
  dependencies:
    - composer_install:php83

